// BUNKER - Vault-Tec AI Command Center
// Tauri 2.0 Desktop Application
// Phase 1: UI with mock data
// Phase 2: Real system monitoring via Rust backend

#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]

fn main() {
    bunker_lib::run()
}
